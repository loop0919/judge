#!/bin/sh
set -eu

cd "$(git rev-parse --show-toplevel)"
exec nix develop . --command sh -ec '
  cd web
  npm run typecheck
  npm run build
  results=$(mktemp -d "${TMPDIR:-/tmp}/judge-pre-push.XXXXXX")
  echo "Playwright results: $results"
  npm test -- --output="$results"
' </dev/null >&2
